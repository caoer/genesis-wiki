---
source: "{{PROVENANCE — a wiki:// pin, a sources/ path, or a URL; every claim page names where it came from (contract `C24-reference-classes`)}}"
tags: [domain/example]
created: {{YYYY-MM-DD}}
---

# grind-size

Finer grind → more extraction → bitterness; coarser → less extraction → sourness. Dial one variable at a time.

What this leaf demonstrates: one page, one topic; frontmatter carries provenance, body carries the claim; clause citations are number+slug (contract `C39-clause-citation`) or absent — never a bare number.
